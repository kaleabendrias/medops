# unit_tests

Containerized unit test entrypoints:

- `run_backend_unit_tests.sh` runs Rust unit tests for `api-service`.
- `run_frontend_unit_tests.sh` runs Rust unit tests for `web-app`.

Both scripts emit machine-readable JSON into the provided report directory.
